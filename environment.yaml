name: 3DMOT-Nerf-lightning
channels:
    - pytorch
    - nvidia
dependencies:
    - python=3.10
    - pip
    - pytorch
    - pytorch-cuda=11.8
    - torchvision
    - pip:
        - scipy
        - pytorch-lightning
        - tqdm
        - opencv-python
        - imageio
        - imageio-ffmpeg
        - configargparse
        - wandb
        - git+https://github.com/princeton-vl/lietorch.git

